<!--Create a new final pic here-->
Create a new final pic here <br>

<%= simple_form_for [@benchmark_pic, @final_pic] do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :photo, as: :file %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

<div class="photo-container">
  <%= cl_image_tag @benchmark_pic.photo.key, class: 'benchmark_pic' %>
  <video autoplay="" muted="" playsinline="" class="selfie-video">
          Video stream not available.
  </video>
  <p class="selfie-startbutton">📸</p>
</div>
